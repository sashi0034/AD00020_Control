<Window x:Class="AD00020_Control.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:local="clr-namespace:AD00020_Control"
        mc:Ignorable="d"
        Title="AD00020 Control" Height="450" Width="800"
        ui:WindowHelper.UseModernWindowStyle="True"
        ResizeMode="CanResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />  <!-- 操作上段 -->
            <RowDefinition Height="Auto" />  <!-- 操作下段 -->
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />     <!-- ログ -->
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock>Device Status:</TextBlock>
                <TextBlock x:Name="StatusText" Margin="10 0"></TextBlock>
            </StackPanel>
        </Grid>


        <!-- 上段 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />     <!-- ON/OFF -->
                <ColumnDefinition Width="Auto" />  <!-- Settings -->
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Power: " VerticalAlignment="Center" FontSize="14" />

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <Button x:Name="PowerOnButton" Content="ON" Width="80" Height="32" Margin="2 0" FontSize="14"
                        Click="PowerOnButton_OnClick" />
                <Button x:Name="PowerOffButton" Content="OFF" Width="80" Height="32" Margin="2 0" FontSize="14"
                        Click="PowerOffButton_OnClick" />
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button x:Name="OpenSettingsButton" Content="Open settings.json" Width="Auto" Height="32" Margin="2 0"
                        FontSize="14" Click="OpenSettingsButton_OnClick" />
                <Button x:Name="ReloadSettingsButton" Content="Reload settings.json" Width="Auto" Height="32"
                        Margin="2 0" FontSize="14"
                        Click="ReloadSettingsButton_OnClick" />
            </StackPanel>
        </Grid>

        <!-- 下段：Job 表示 + トグルボタン -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0 8 0 0" HorizontalAlignment="Left"
                    VerticalAlignment="Center">
            <TextBlock Text="Job:" VerticalAlignment="Center" FontSize="14" Margin="0,0,8,0" />
            <Button x:Name="JobToggleButton" Content="Start Job" Width="100" Height="32" FontSize="14"
                    Click="JobToggleButton_OnClick" />
        </StackPanel>

        <!-- コマンド入力欄 -->
        <Grid Grid.Row="3" Margin="0 8 0 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="CommandInput" Grid.Column="0" Height="32" FontSize="14"
                     VerticalContentAlignment="Center"
                     KeyDown="CommandInput_OnKeyDown" />

            <Button x:Name="CommandExecuteButton" Grid.Column="1" Content="Command"
                    Width="Auto" Height="32" Margin="8 0 0 0"
                    FontSize="14" Click="CommandExecuteButton_OnClick" />

            <Button x:Name="CommandCancelButton" Grid.Column="2" Content="Cancel"
                    Width="Auto" Height="32" Margin="8 0 0 0"
                    FontSize="14" Click="CommandCancelButton_OnClick" />
        </Grid>

        <!-- ログ -->
        <GroupBox Grid.Row="4">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0 0" Padding="0">
                <TextBox x:Name="LoggerText" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         FontSize="14" Margin="0" />
            </ScrollViewer>
        </GroupBox>
    </Grid>
</Window>